1. What is the error in this statement?

	if scoreA = scoreB :  
        		print("Tie")
 # (=) sign assign if we want to said equal , we have to use (==) 
--------------------------------------------------------------------------------------------------
2. Supply a condition in this if statement to test if the user entered a “Y”:
	userInput = input("Enter Y to quit.")
	if . . . // supply statement
		print("Goodbye")    // if the user entered “Y”
--------------------------------------------------------------------------------------------------
# userInput = input("Enter Y to quit.)
  if userInput.upper() == "Y" :
     print ("Goodbye")
--------------------------------------------------------------------------------------------------    
     
3. Find the errors in the following if statements, correct where necessary.

a) if x > 0 then :
    	print(x)
 #  No need to put “then”, just the (:)
 
    if x > 0:
    print(x)
 --------------------------------------------------------------------------------------------------   
 3. b) if 1 + x > x ** sqrt(2) :
       y = y + x
      
   # we need to import math , define x and y and print (y)
   
   # from math import sqrt, pow, pi
     x = 1
     y = 9
     if 1 + x > x ** sqrt(2) :
       y = y + x
       print(y)
 --------------------------------------------------------------------------------------------------  
 3.c) if x = 1 :
    	y += 1
#  x == 1 instead of x = 1

